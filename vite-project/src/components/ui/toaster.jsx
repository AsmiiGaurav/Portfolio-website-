import React from 'react'

// Minimal placeholder Toaster component so the app can import it without errors.
// Replace with your full toast implementation later (e.g., react-hot-toast or custom UI).
export const Toaster = () => {
  return null
}

export default Toaster
